# Optional. By default the app derives the gateway host from the Expo dev server,
# which works for the Android emulator, the iOS simulator and devices on the same LAN.
# EXPO_PUBLIC_GATEWAY_URL=http://192.168.1.10:4000
# EXPO_PUBLIC_GATEWAY_PORT=4000
